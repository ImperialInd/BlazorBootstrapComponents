@{
	string _class = string.Concat(Class, (Class.Length > 0 ? " " : ""),
									GenerateWidth(),
									GenerateOffset(),
									GenerateOrder(),
									GenerateVerticalAlignment()).Trim();
	<div class="@_class">
		@ChildContent
	</div>
}